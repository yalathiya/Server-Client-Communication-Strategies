version: "3"

services:
  api:
    build: .
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/usr/src/app
    networks:
      - api-network

networks:
  api-network:
    driver: bridge